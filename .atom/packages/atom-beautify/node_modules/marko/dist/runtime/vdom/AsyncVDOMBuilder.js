var EventEmitter = require('events-light');
var vdom = require('./vdom');
var VElement = vdom.ah_;
var VDocumentFragment = vdom.aC_;
var VComment = vdom.aD_;
var VText = vdom.aE_;
var VComponent = vdom.aF_;
var virtualizeHTML = vdom.aG_;
var RenderResult = require('../RenderResult');
var defaultDocument = vdom.aH_;
var morphdom = require('../../morphdom');

var EVENT_UPDATE = 'update';
var EVENT_FINISH = 'finish';

function State(tree) {

    this.aI_ = new EventEmitter();
    this.aJ_ = tree;
    this.aK_ = false;
}

function AsyncVDOMBuilder(globalData, parentNode, parentOut) {
    if (!parentNode) {
        parentNode = new VDocumentFragment();
    }

    var state;

    if (parentOut) {
        state = parentOut.g_;
    } else {
        state = new State(parentNode);
    }

    this.aL_ = 1;
    this.aM_ = 0;
    this.aN_ = null;
    this.aO_ = parentOut;

    this.data = {};
    this.g_ = state;
    this.a__ = parentNode;
    this.global = globalData || {};
    this.aP_ = [parentNode];
    this.aQ_ = false;
    this.aR_ = undefined;
    this._r_ = null;

    this._V_ = null;
    this._X_ = null;
    this._W_ = null;
}

var proto = AsyncVDOMBuilder.prototype = {
    ay_: true,
    v_: defaultDocument,

    bc: function (component) {
        var vComponent = new VComponent(component);
        return this.aS_(vComponent, 0, true);
    },

    _Z_: function (component) {
        var vComponent = new VComponent(component, true);
        this.aS_(vComponent, 0);
    },

    aS_: function (child, childCount, pushToStack) {
        this.a__.aT_(child);
        if (pushToStack === true) {
            this.aP_.push(child);
            this.a__ = child;
        }
        return childCount === 0 ? this : child;
    },

    element: function (tagName, attrs, key, component, childCount, flags, props) {
        var element = new VElement(tagName, attrs, key, component, childCount, flags, props);
        return this.aS_(element, childCount);
    },

    aU_: function (tagName, attrs, key, component, childCount, flags, props) {
        var element = VElement.aV_(tagName, attrs, key, component, childCount, flags, props);
        return this.aS_(element, childCount);
    },

    n: function (node, component) {
        // NOTE: We do a shallow clone since we assume the node is being reused
        //       and a node can only have one parent node.
        var clone = node.aW_();
        this.node(clone);
        clone._a_ = component;

        return this;
    },

    node: function (node) {
        this.a__.aT_(node);
        return this;
    },

    text: function (text) {
        var type = typeof text;

        if (type != 'string') {
            if (text == null) {
                return;
            } else if (type === 'object') {
                if (text.toHTML) {
                    return this.h(text.toHTML());
                }
            }

            text = text.toString();
        }

        this.a__.aT_(new VText(text));
        return this;
    },

    comment: function (comment) {
        return this.node(new VComment(comment));
    },

    html: function (html) {
        if (html != null) {
            var vdomNode = virtualizeHTML(html, this.v_ || document);
            this.node(vdomNode);
        }

        return this;
    },

    beginElement: function (tagName, attrs, key, component, childCount, flags, props) {
        var element = new VElement(tagName, attrs, key, component, childCount, flags, props);
        this.aS_(element, childCount, true);
        return this;
    },

    aX_: function (tagName, attrs, key, component, childCount, flags, props) {
        var element = VElement.aV_(tagName, attrs, key, component, childCount, flags, props);
        this.aS_(element, childCount, true);
        return this;
    },

    endElement: function () {
        var stack = this.aP_;
        stack.pop();
        this.a__ = stack[stack.length - 1];
    },

    end: function () {
        this.a__ = undefined;

        var remaining = --this.aL_;
        var parentOut = this.aO_;

        if (remaining === 0) {
            if (parentOut) {
                parentOut.aY_();
            } else {
                this.aZ_();
            }
        } else if (remaining - this.aM_ === 0) {
            this.b__();
        }

        return this;
    },

    aY_: function () {
        var remaining = --this.aL_;

        if (remaining === 0) {
            var parentOut = this.aO_;
            if (parentOut) {
                parentOut.aY_();
            } else {
                this.aZ_();
            }
        } else if (remaining - this.aM_ === 0) {
            this.b__();
        }
    },

    aZ_: function () {
        var state = this.g_;
        state.aK_ = true;
        state.aI_.emit(EVENT_FINISH, this.az_());
    },

    b__: function () {
        var lastArray = this._last;

        var i = 0;

        function next() {
            if (i === lastArray.length) {
                return;
            }
            var lastCallback = lastArray[i++];
            lastCallback(next);

            if (!lastCallback.length) {
                next();
            }
        }

        next();
    },

    error: function (e) {
        try {
            this.emit('error', e);
        } finally {
            // If there is no listener for the error event then it will
            // throw a new Error here. In order to ensure that the async fragment
            // is still properly ended we need to put the end() in a `finally`
            // block
            this.end();
        }

        return this;
    },

    beginAsync: function (options) {
        if (this.aQ_) {
            throw Error('Not allowed');
        }

        var state = this.g_;

        if (options) {
            if (options.last) {
                this.aM_++;
            }
        }

        this.aL_++;

        var documentFragment = this.a__.ba_();
        var asyncOut = new AsyncVDOMBuilder(this.global, documentFragment, this);

        state.aI_.emit('beginAsync', {
            out: asyncOut,
            parentOut: this
        });

        return asyncOut;
    },

    createOut: function () {
        return new AsyncVDOMBuilder(this.global);
    },

    flush: function () {
        var events = this.g_.aI_;

        if (events.listenerCount(EVENT_UPDATE)) {
            events.emit(EVENT_UPDATE, new RenderResult(this));
        }
    },

    S_: function () {
        return this.g_.aJ_;
    },

    az_: function () {
        return this.bb_ || (this.bb_ = new RenderResult(this));
    },

    on: function (event, callback) {
        var state = this.g_;

        if (event === EVENT_FINISH && state.aK_) {
            callback(this.az_());
        } else if (event === 'last') {
            this.onLast(callback);
        } else {
            state.aI_.on(event, callback);
        }

        return this;
    },

    once: function (event, callback) {
        var state = this.g_;

        if (event === EVENT_FINISH && state.aK_) {
            callback(this.az_());
        } else if (event === 'last') {
            this.onLast(callback);
        } else {
            state.aI_.once(event, callback);
        }

        return this;
    },

    emit: function (type, arg) {
        var events = this.g_.aI_;
        switch (arguments.length) {
            case 1:
                events.emit(type);
                break;
            case 2:
                events.emit(type, arg);
                break;
            default:
                events.emit.apply(events, arguments);
                break;
        }
        return this;
    },

    removeListener: function () {
        var events = this.g_.aI_;
        events.removeListener.apply(events, arguments);
        return this;
    },

    sync: function () {
        this.aQ_ = true;
    },

    isSync: function () {
        return this.aQ_;
    },

    onLast: function (callback) {
        var lastArray = this._last;

        if (lastArray === undefined) {
            this._last = [callback];
        } else {
            lastArray.push(callback);
        }

        return this;
    },

    aw_: function (doc) {
        var node = this.aR_;
        if (!node) {
            var vdomTree = this.S_();
            // Create the root document fragment node
            doc = doc || this.v_ || document;
            this.aR_ = node = vdomTree.al_(doc);
            morphdom(node, null, null, vdomTree, doc, this._r_);
        }
        return node;
    },

    toString: function (doc) {
        var docFragment = this.aw_(doc);
        var html = '';

        var child = docFragment.firstChild;
        while (child) {
            var nextSibling = child.nextSibling;
            if (child.nodeType != 1) {
                var container = docFragment.ownerDocument.createElement('div');
                container.appendChild(child.cloneNode());
                html += container.innerHTML;
            } else {
                html += child.outerHTML;
            }

            child = nextSibling;
        }

        return html;
    },

    then: function (fn, fnErr) {
        var out = this;
        var promise = new Promise(function (resolve, reject) {
            out.on('error', reject).on(EVENT_FINISH, function (result) {
                resolve(result);
            });
        });

        return Promise.resolve(promise).then(fn, fnErr);
    },

    catch: function (fnErr) {
        return this.then(undefined, fnErr);
    },

    isVDOM: true,

    c: function (componentDef, key, customEvents) {
        this._V_ = componentDef;
        this._X_ = key;
        this._W_ = customEvents;
    }
};

proto.e = proto.element;
proto.ed = proto.aU_;
proto.be = proto.beginElement;
proto.bed = proto.aX_;
proto.ee = proto.endElement;
proto.t = proto.text;
proto.h = proto.w = proto.write = proto.html;

module.exports = AsyncVDOMBuilder;