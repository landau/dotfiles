var VNode = require('./VNode');
var inherit = require('raptor-util/inherit');

function VComment(value) {
    this.aL_(-1 /* no children */);
    this.ae_ = value;
}

VComment.prototype = {
    ac_: 8,

    ab_: function (doc) {
        return doc.createComment(this.ae_);
    },

    aG_: function () {
        return new VComment(this.ae_);
    }
};

inherit(VComment, VNode);

module.exports = VComment;