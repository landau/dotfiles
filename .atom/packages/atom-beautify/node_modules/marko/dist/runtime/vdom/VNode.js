/* jshint newcap:false */
function VNode() {}

VNode.prototype = {
    bc_: function (finalChildCount) {
        this.bn_ = finalChildCount;
        this.bo_ = 0;
        this.bg_ = null;
        this.bp_ = null;
        this.bd_ = null;
        this.be_ = null;
    },

    _a_: null,

    get ap_() {
        var firstChild = this.bg_;

        if (firstChild && firstChild.bf_) {
            var nestedFirstChild = firstChild.ap_;
            // The first child is a DocumentFragment node.
            // If the DocumentFragment node has a first child then we will return that.
            // Otherwise, the DocumentFragment node is not *really* the first child and
            // we need to skip to its next sibling
            return nestedFirstChild || firstChild.an_;
        }

        return firstChild;
    },

    get an_() {
        var nextSibling = this.be_;

        if (nextSibling) {
            if (nextSibling.bf_) {
                var firstChild = nextSibling.ap_;
                return firstChild || nextSibling.an_;
            }
        } else {
            var parentNode = this.bd_;
            if (parentNode && parentNode.bf_) {
                return parentNode.an_;
            }
        }

        return nextSibling;
    },

    aT_: function (child) {
        this.bo_++;

        if (this.bk_ === true) {
            if (child.bq_) {
                var childValue = child.as_;
                this.bj_ = (this.bj_ || '') + childValue;
            } else {
                throw TypeError();
            }
        } else {
            var lastChild = this.bp_;

            child.bd_ = this;

            if (lastChild) {
                lastChild.be_ = child;
            } else {
                this.bg_ = child;
            }

            this.bp_ = child;
        }

        return child;
    },

    bl_: function finishChild() {
        if (this.bo_ === this.bn_ && this.bd_) {
            return this.bd_.bl_();
        } else {
            return this;
        }
    }

    // ,toJSON: function() {
    //     var clone = Object.assign({
    //         nodeType: this.nodeType
    //     }, this);
    //
    //     for (var k in clone) {
    //         if (k.startsWith('_')) {
    //             delete clone[k];
    //         }
    //     }
    //     delete clone._nextSibling;
    //     delete clone._lastChild;
    //     delete clone.parentNode;
    //     return clone;
    // }
};

module.exports = VNode;