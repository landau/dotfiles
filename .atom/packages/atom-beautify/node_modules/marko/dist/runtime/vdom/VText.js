var VNode = require('./VNode');
var inherit = require('raptor-util/inherit');

function VText(value) {
    this.aL_(-1 /* no children */);
    this.ae_ = value;
}

VText.prototype = {
    aH_: true,

    ac_: 3,

    ab_: function (doc) {
        return doc.createTextNode(this.ae_);
    },

    aG_: function () {
        return new VText(this.ae_);
    }
};

inherit(VText, VNode);

module.exports = VText;