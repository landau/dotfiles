var VNode = require('./VNode');
var inherit = require('raptor-util/inherit');

function VText(value) {
    this.bc_(-1 /* no children */);
    this.as_ = value;
}

VText.prototype = {
    bq_: true,

    am_: 3,

    al_: function (doc) {
        return doc.createTextNode(this.as_);
    },

    aW_: function () {
        return new VText(this.as_);
    }
};

inherit(VText, VNode);

module.exports = VText;