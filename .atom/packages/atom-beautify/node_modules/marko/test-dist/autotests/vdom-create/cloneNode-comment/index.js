var expect = require('chai').expect;

module.exports = function (helpers) {
    var comment = helpers.vdom.createComment('This is a comment');
    var commentClone = comment.aG_();
    expect(commentClone).to.not.equal(comment);
    expect(comment.ae_).to.equal('This is a comment');
    expect(commentClone.ae_).to.equal('This is a comment');
    return comment;
};